import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-phrase-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
